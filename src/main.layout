<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="icon" href="{{cdn}}/{{builder}}@{{builder_version}}/img/L.png">
    <link rel="stylesheet" href="{{cdn}}/purecss@1.0.0/build/pure-min.css">
    <link rel="stylesheet" href="{{cdn}}/{{builder}}@{{builder_version}}/stylesheets/main.css">
    <title>Department of Logic – {{name}}</title>
</head>
<body>

<header>
<h1>{{name}}</h1>
{{menu}}
</header>

<main>
{{{yield}}}
</main>

</body>
</html>
